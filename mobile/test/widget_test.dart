// Smoke test placeholder. The cockpit UI requires a live gateway connection
// and is exercised manually (flutter run -d windows) in Task 14 verification.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
